# Multi-WAN Gateway - sysctl settings
# Deploy to /etc/sysctl.d/90-multiwan.conf

# Enable IP forwarding (gateway mode)
net.ipv4.ip_forward=1

# L4 hash policy: each TCP connection always uses the same path
# Prevents packet reordering across WAN links
net.ipv4.fib_multipath_hash_policy=1
